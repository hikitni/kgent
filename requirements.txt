watchdog>=4.0.0
rich>=13.0.0

# Web 管理界面依赖
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
jinja2>=3.0.0
python-multipart>=0.0.9

# 测试依赖
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# AI 总结依赖（按需安装一个）：
# zhipuai>=2.0.0          # 智谱 GLM（ai_provider=zhipuai）
#   Python 3.14+ 需同时安装: pip install sniffio
# openai>=1.0.0           # OpenAI / DeepSeek / Moonshot 等兼容接口（ai_provider=openai）
# Ollama 无需额外安装（ai_provider=ollama，纯 HTTP）
